Palavras-Chave: gtk, file access in C, proc directory in linux, GUI

Programa Simples Em C, para exibir, o modelo do processador e total o de memoria fisica.

O Programa exibe essas Informacoes em uma GUI.

    Pre-requisitos:
        SO: Unix-like(Debian, Ubunto, etc..)
        Pacotes:    libgtk-3-dev (pode ser instalado com o comando: sudo apt-get install libgtk-3-dev);
                    gcc;
